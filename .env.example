# Copy to .env and adjust
APP_PORT=8000

# Postgres (async)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/amnezia_manager

# Redis for ARQ queue
REDIS_URL=redis://redis:6379/0

# 32-byte urlsafe base64 key for Fernet encryption (generate with: python -c "import base64,secrets;print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())")
ENCRYPTION_KEY=EcN9SwhO7VbphDAgwlCTOgDWFtQ5wwc-9yyJLb3a6GY=

# Optional
LOG_LEVEL=INFO
REMOTE_DEPLOY_DIR=/opt/amneziawg-web-ui
DEFAULT_WEB_UI_PORT=8080
DEFAULT_WG_PORT=51820
DEFAULT_NGINX_USER=admin
AWG_IMAGE=alexishw/amneziawg-web-ui:latest
ACCEPT_UNKNOWN_SSH_HOST_KEYS=true
